# Core UI and visualization
streamlit==1.36.0
pytest==8.2.2
plotly==5.22.0

# Core HTTP requests
requests==2.31.0

# Environment variable management
python-dotenv==1.0.1

# JSON parsing and validation
jsonschema==4.22.0

# Data validation models (agents/impact_assessment_agent.py, utils/models.py)
pydantic==2.7.4

# Logging (built-in, but structlog adds structured logging if needed)
structlog==24.1.0

# Caching (utils/cache.py — in-memory + SQLite, no extra dependency needed)

# Data handling
pandas==2.2.2
numpy==1.26.4

# Date/time utilities
python-dateutil==2.9.0.post0

# Typing support
typing-extensions==4.12.2

# Vector database for RAG. Pinned to 5.x for pc.inference.embed() (hosted
# embeddings matching the existing indexes' llama-text-embed-v2 model) —
# 3.2.2 predates that API; 6.0.0+ renames the package to `pinecone`.
pinecone-client==5.0.1
